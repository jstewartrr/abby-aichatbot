version: 1.3.1
cache: true

interface:
  customName: "HiveMind Chat"
  privacyPolicy:
    externalUrl: "https://abbi-ai.com/privacy"
  termsOfService:
    externalUrl: "https://abbi-ai.com/terms"
  endpointsMenu: true
  modelSelect: true
  parameters: true
  presets: true
  sidePanel: true
  mcpServers:
    use: true
    create: true
    share: true

# MCP Server Configuration
mcpServers:
  sovereign-mind-gateway:
    type: sse
    url: "https://cv-sm-gateway-v3.lemoncoast-87756bcf.eastus.azurecontainerapps.io/sse"
    timeout: 60000
    initTimeout: 30000
    serverInstructions: true

registration:
  socialLogins: []
  allowedDomains:
    - "middleground.com"
    - "abbi-ai.com"

rateLimits:
  fileUploads:
    ipMax: 100
    ipWindowInMinutes: 60
    userMax: 50
    userWindowInMinutes: 60

# Endpoint Configuration
endpoints:
  # Agents Configuration
  agents:
    disableBuilder: false
    capabilities:
      - "execute_code"
      - "file_search"
      - "web_search"
      - "artifacts"
      - "actions"
      - "context"
      - "tools"
      - "chain"
      - "ocr"

  # Built-in Anthropic (direct API)
  anthropic:
    streamRate: 25
    titleModel: "claude-3-5-haiku-20241022"

  # Built-in Google (Gemini via AI Studio)
  google:
    streamRate: 25
    titleModel: "gemini-1.5-flash"

  # AWS Bedrock
  bedrock:
    streamRate: 35
    titleModel: "us.amazon.nova-lite-v1:0"
    availableRegions:
      - "us-east-1"

  # Azure OpenAI
  azureOpenAI:
    titleModel: "gpt-4o"
    groups:
      - group: "sovereign-mind"
        apiKey: "${AZURE_OPENAI_API_KEY}"
        instanceName: "sovereign-mind-openai"
        version: "2024-08-01-preview"
        models:
          gpt-4o:
            deploymentName: "gpt-4o"

  # Disable assistants
  assistants:
    disabled: true

  # Custom Endpoints
  custom:
    # OpenAI
    - name: "OpenAI"
      apiKey: "${OPENAI_API_KEY}"
      baseURL: "https://api.openai.com/v1"
      models:
        default: ["gpt-4o", "gpt-4o-mini", "o1", "o1-mini", "o3-mini"]
        fetch: false
      titleConvo: true
      titleModel: "gpt-4o-mini"
      modelDisplayLabel: "OpenAI"

    # Grok (xAI)
    - name: "Grok"
      apiKey: "${XAI_API_KEY}"
      baseURL: "https://api.x.ai/v1"
      models:
        default: ["grok-3", "grok-3-fast", "grok-2-1212"]
        fetch: false
      titleConvo: true
      titleModel: "grok-3-fast"
      modelDisplayLabel: "Grok (xAI)"

# File Configuration
fileConfig:
  endpoints:
    openAI:
      fileLimit: 10
      fileSizeLimit: 512
      disabled: false
    anthropic:
      fileLimit: 10
      fileSizeLimit: 100
      disabled: false
    default:
      fileLimit: 5
      fileSizeLimit: 50
